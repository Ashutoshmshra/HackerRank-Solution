class Printer
{ 
   public static < E > void printArray( E[] inputArray ) {
      // Display array elements
      for(E element : inputArray) {
         System.out.printf("%s \n", element);
      }
     // System.out.println();
} 
}
